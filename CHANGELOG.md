# Changelog

Версия сайта дублируется в `package.json` и в шапке `README.md`.

## 0.2.0 — 2026-08-22

FAQ-ассистент Аркадия и карточка LoRA.

- FAQ вынесен в `lib/knowledge/faqs.json`: один источник для блока на сайте и для поиска в чате.
- Поиск смотрит FAQ-пары и текстовые материалы; уточнения вроде «а сколько стоит?» учитывают предыдущий вопрос.
- Виджет показывает 1–2 источника под ответом.
- Добавлена карточка кейса `style-lora-mentor` (пайплайн дообучения, не «умная модель»).
- Из публичной базы убраны внутренние статусы CRM.

После деплоя на сервере, где есть `OPENAI_API_KEY`, пересобрать индекс: `npm run rag:index`.

## 0.1.0

Первый публичный сайт: бренд, кейсы, форма заявки, чат Аркадия.
